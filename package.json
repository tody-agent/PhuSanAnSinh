{
  "name": "phusanansinh",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tests/*.test.js",
    "test:gate": "node --test tests/*.test.js",
    "deploy": "npm run test:gate && npm run build && npx wrangler pages deploy dist --project-name phusanansinh --branch main"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/mdx": "^4.3.13",
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.18.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
