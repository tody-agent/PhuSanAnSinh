name = "ansinh-api"
main = "src/index.js"
compatibility_date = "2024-03-14"
compatibility_flags = ["nodejs_compat"]

[vars]
DEFAULT_TENANT = "an-sinh"

# Secrets (set via: wrangler secret put DATABASE_URL / JWT_SECRET)
# DATABASE_URL = "postgresql://..."
# JWT_SECRET = "your-jwt-secret"
